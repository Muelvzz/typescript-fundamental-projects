export function action(command: string) {
    if (command === 'attack') {
        console.log('You entered attack')
    }

    if (command === 'heal') {
        console.log('You entered heal')
    }
}